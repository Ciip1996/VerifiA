import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('smoke test', (WidgetTester tester) async {
    // Placeholder — real integration tests live in the backend test suite.
    expect(true, isTrue);
  });
}
